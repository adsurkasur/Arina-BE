{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "build:server": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "postbuild": "cp -r ../client/dist/* ./public/",
    "build": "npm run build:client && npm run build:server && npm run postbuild",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "arina-shared": "git+https://github.com/adsurkasur/Arina-SharedPackage.git"
  }
}